{{#with comment}}
    {{#if hasVisibleComponent}}
        &mdash;
        <div class="tsd-comment tsd-typography" style="display: inline-block">
            {{#if shortText}}
                <div class="lead">
                    {{#markdown}}{{{shortText}}}{{/markdown}}
                </div>
            {{/if}}
            {{#if text}}
                {{#markdown}}{{{text}}}{{/markdown}}
            {{/if}}
            {{#if tags}}
                <dl class="tsd-comment-tags">
                    {{#each tags}}
                        <dt>{{tagName}}</dt>
                        <dd>{{#markdown}}{{{text}}}{{/markdown}}</dd>
                    {{/each}}
                </dl>
            {{/if}}
        </div>
    {{/if}}
{{/with}}